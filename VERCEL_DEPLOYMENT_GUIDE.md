# Vercel 포트폴리오 배포 가이드

이 가이드는 Next.js와 React를 사용한 포트폴리오 웹사이트를 Vercel에 배포하는 방법을 단계별로 설명합니다.

## 📋 목차

1. [프로젝트 준비](#1-프로젝트-준비)
2. [Git 저장소 생성](#2-git-저장소-생성)
3. [Vercel 계정 생성 및 로그인](#3-vercel-계정-생성-및-로그인)
4. [프로젝트 배포](#4-프로젝트-배포)
5. [배포 완료 후 도메인 확인](#5-배포-완료-후-도메인-확인)
6. [배포 자동화](#6-배포-자동화)

---

## 1. 프로젝트 준비

### 1.1 Next.js 프로젝트 개발
- Next.js와 React를 사용해 로컬에서 포트폴리오 웹사이트를 개발합니다.
- Tailwind CSS 등 원하는 스타일링 도구를 적용해 디자인을 완성합니다.

### 1.2 로컬 테스트
```bash
# 개발 서버 실행
npm run dev
# 또는
yarn dev
# 또는
pnpm dev
```

로컬에서 정상적으로 잘 동작하는지 확인하세요.

---

## 2. Git 저장소 생성

### 2.1 원격 저장소 생성
GitHub, GitLab, 또는 Bitbucket에 프로젝트 소스코드를 올릴 원격 저장소를 만듭니다.

### 2.2 로컬 Git 초기화 및 커밋
프로젝트 폴더에서 다음 명령어를 실행합니다:

```bash
# Git 초기화
git init

# 모든 파일 추가
git add .

# 초기 커밋 생성
git commit -m "initial commit"

# 원격 저장소 연결 (예: GitHub)
git remote add origin https://github.com/사용자명/저장소명.git

# 메인 브랜치 설정
git branch -M main

# 원격 저장소에 푸시
git push -u origin main
```

---

## 3. Vercel 계정 생성 및 로그인

1. [https://vercel.com](https://vercel.com)에서 회원가입 후 로그인합니다.
2. GitHub, GitLab 등과 연동할 수 있습니다.

---

## 4. 프로젝트 배포

### 4.1 Vercel 대시보드에서 프로젝트 생성
1. Vercel 대시보드에서 **"New Project"**를 클릭합니다.
2. 원격 저장소(GitHub 등)를 연결해 배포할 프로젝트를 선택합니다.

### 4.2 배포 설정
- 기본 설정으로 Next.js 프로젝트를 자동 감지하므로 특별한 설정 없이 **"Deploy"** 버튼을 누릅니다.
- 필요시 다음 설정을 확인/수정할 수 있습니다:
  - **Framework Preset**: Next.js (자동 감지)
  - **Root Directory**: 프로젝트 루트 디렉토리
  - **Build Command**: `npm run build` (기본값)
  - **Output Directory**: `.next` (기본값)
  - **Install Command**: `npm install` (기본값)

### 4.3 환경 변수 설정 (필요시)
환경 변수가 필요한 경우 Vercel 대시보드의 프로젝트 설정에서 추가할 수 있습니다.

---

## 5. 배포 완료 후 도메인 확인

배포가 완료되면:
- Vercel에서 제공하는 임시 도메인 (예: `프로젝트명.vercel.app`)에서 사이트가 정상적으로 작동하는지 확인합니다.
- 커스텀 도메인을 연결한 경우 해당 도메인에서도 확인합니다.

---

## 6. 배포 자동화

이후 Git 저장소에 수정 사항을 커밋하고 푸시하면 Vercel이 자동으로 재배포를 수행합니다.

```bash
# 변경사항 커밋
git add .
git commit -m "업데이트 내용"

# 원격 저장소에 푸시 (자동 배포 트리거)
git push
```

---

## 📝 요약

1. ✅ 로컬에서 Next.js 포트폴리오를 만듭니다.
2. ✅ 소스코드를 GitHub 같은 원격 저장소에 푸시합니다.
3. ✅ Vercel과 연결해 바로 배포합니다.
4. ✅ Vercel이 Next.js 환경에 최적화되어 있어 설정 없이도 쉽게 배포할 수 있습니다.

이런 순서를 따라가면 멋진 포트폴리오 사이트를 Vercel에 올릴 수 있습니다! 🚀

---

## 🔧 문제 해결

### 빌드 오류가 발생하는 경우
- `package.json`의 `scripts` 섹션을 확인하세요.
- 로컬에서 `npm run build`가 성공하는지 확인하세요.

### 환경 변수 오류
- Vercel 대시보드의 프로젝트 설정 > Environment Variables에서 필요한 변수를 추가하세요.

### 도메인 연결 문제
- DNS 설정이 올바른지 확인하세요.
- Vercel의 도메인 설정 가이드를 참고하세요.

---

## 📚 참고 자료

- [Vercel 공식 문서](https://vercel.com/docs)
- [Next.js 배포 가이드](https://nextjs.org/docs/deployment)
- [Git 기본 명령어](https://git-scm.com/docs)

